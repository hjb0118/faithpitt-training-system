# 2026-09-23 工作日志

> 制度说明：每次代码/数据/配置变更后，都要在 `logs/` 下写当天日志（或追加到当日文件），格式参照本文件。

## 今日工作：全量代码审查 + 安全加固（3 项）+ 环境修复 + 建立记忆/日志制度

### 1. 全量代码审查
- 通读全部代码：server.js、db.js、db-adapter.js、wechat_notify.js、index.html、js/app.js（5163 行）、js/icons.js、css/main.css、deploy.yml、sync_to_server.py、migrate*.js、package.json、.gitignore、运行部署.bat、README.md。
- 对照 PROJECT_CONTEXT.md 实查 training.db：49 条记录 / 34 用户 / 347 日志 / 5 部门 / nextId=65，状态分布（已通过 42、30天已回访 6、已完成 1），与文档一致。records 实际 38 列（含 `archived`）。

### 2. 发现并修复的安全问题（本次核心）
| # | 问题 | 修法 | 实测验证 |
|---|---|---|---|
| 1 | 静态文件服务可未认证下载任意项目文件（config.local.json 含全部凭据 / training.db / tokens.json / backups/） | server.js 静态服务改白名单（仅 index.html、css/、js/），先解析真实路径再校验 | 6 个敏感路径（含 `/js/../training.db` 穿透）全部 404，正常页面/样式/脚本 200 |
| 2 | register 接口可用 `_role:'setup'` 绕过"仅首次注册"，未认证创建 HR 账号 | 去掉绕过条件，仅用户表为空时允许注册 | 伪造注册返回 `{"ok":false,"msg":"not first"}`，未产生账号 |
| 3 | 服务器 root 密码明文硬编码在 server.js（pullFromCloud）和 sync_to_server.py（已进 git 历史） | 两处改从 config.local.json 的 `server_pass` 读取，未配置时明确报错 | grep 确认代码零残留 |
| 附带 | `/api/error-report`、`/api/v1` 被静态处理器拦截不可达 | 同根因，静态处理器不再拦截 `/api/*` | 两路由恢复（error-report 返回 ok，/api/v1 返回 JSON） |

### 3. 环境修复
- better-sqlite3 原按 Node 22 编译，默认 `node`（v24）加载报 ERR_DLOPEN_FAILED。已按 **Node 22** 重建模块，并用 `C:\Users\PC\.workbuddy\binaries\node\versions\22.22.2-3\node.exe server.js` 启动本地服务，运行正常（9 个会话恢复）。
- 注意：默认 `node` 仍是 v24，直接 `node server.js` 会报错，必须用上面的 Node 22 路径启动。

### 4. 文档与制度
- PROJECT_CONTEXT.md：安全机制表加"静态文件白名单"、8.5 补密码外置说明、版本时间线补记录、已知问题表新增 10~15 条（含未修问题）。
- 新建 `MEMORY.md`（AI 总记忆：架构概括 + 工作流，新会话先读它）。
- 新建 `logs/` 每日日志制度（本文件是第一篇）。
- 确立工作流：改代码 → 检查 → 部署（git push 自动上云 + 云端验证）→ 写当天日志 → 更新 MEMORY.md / PROJECT_CONTEXT.md。

### 5. 部署
- git 提交并 push 到 main → GitHub Actions 自动同步云端并 pm2 重启。
- 云端实测：`GET /training.db`、`/config.local.json` 返回 404（修复前可直接下载）；伪造 register 越权注册被拒。

### ⚠️ 遗留待办（按优先级）
1. **【紧急】轮换阿里云服务器 root 密码**：旧密码已进 git 历史（视为泄露）。换完后同步更新 `config.local.json` 的 `server_pass` 和 GitHub Secrets 的 `SERVER_PASS`。
2. 云端缺 `config.local.json` → 云端 AI 辅助回访不可用（可上传该文件解决）。
3. Excel 批量导入记录失效：前端 POST `/api/apply`，服务端无此端点（方案A 批量建记录正常）→ 待修。
4. `运行部署.bat` 调用不存在的 `deploy_all.py` → 待修。
5. 2 个测试账号（`__TEST__`）可清理。
