# 凡碧诗培训管理系统 — 核心逻辑流

---

## 一、系统架构模式

| 维度 | 实现方式 | 说明 |
|------|----------|------|
| 架构风格 | **单体应用** | 单Node.js进程 + 单HTML前端，无微服务拆分 |
| 前后端通信 | RESTful API + JSON | 标准HTTP请求，无WebSocket/长连接 |
| 数据持久化 | 文件系统（JSON） | data.json读写，无数据库 |
| 状态管理 | 服务端状态机 | 六状态硬编码流转，无工作流引擎 |
| 通知机制 | 同步Webhook调用 | 企微机器人HTTP推送，无消息队列 |
| 定时任务 | 服务端轮询 | 启动时计算倒计时，无cron/job调度器 |

**结论**：这是一个**轻量级单体应用**，技术栈极简，核心逻辑靠代码硬编码实现，**不包含长链推理、多Agent协作、AI决策等复杂机制**。

---

## 二、核心状态机（系统心脏）

```
                    ┌─────────────┐
                    │   待审批     │
                    │  (初始状态)  │
                    └──────┬──────┘
                           │ 员工提交申请
                           ▼
              ┌────────────────────────┐
              │       HR审批            │
              │  通过 → 已通过/学习中    │
              │  驳回 → 结束（可重新申请）│
              └────────────────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │  已通过/学习中│
                    └──────┬──────┘
                           │ 员工完成培训
                           │ 提交学习总结
                           ▼
                    ┌─────────────┐
                    │  总结已提交  │
                    └──────┬──────┘
                           │ +30天到期
                           │ 员工提交30天自评
                           ▼
                    ┌─────────────┐
                    │  30天已回访  │
                    │ HR确认回访结果│
                    └──────┬──────┘
                           │ +90天到期
                           │ 员工提交90天复盘
                           │ HR评估打分
                           ▼
                    ┌─────────────┐
                    │  90天已评估  │
                    └──────┬──────┘
                           │ 状态归档
                           ▼
                    ┌─────────────┐
                    │    已完成    │
                    └─────────────┘
```

**状态流转规则**：
- 所有流转**单向不可逆**
- 每个状态转换由**特定操作触发**（如"提交总结"、"确认回访"）
- 服务端校验权限和前置条件，非法流转直接拒绝

---

## 三、关键业务逻辑流

### 3.1 培训申请流程

```
员工填写表单
    │
    ▼
前端校验（必填项/格式/金额范围）
    │
    ▼
POST /api/records ──→ 服务端 sanitize 输入
    │                    │
    │                    ▼
    │              权限校验（仅员工角色）
    │                    │
    │                    ▼
    │              字段级权限过滤（员工只能写自己的字段）
    │                    │
    │                    ▼
    │              写入 data.json
    │                    │
    │                    ▼
    │              写操作日志
    │                    │
    │                    ▼
    │              触发企微通知（新申请）
    │                    │
    └────────────────────┘
                         ▼
                   返回成功/失败
```

### 3.2 HR审批流程

```
HR点击"通过"或"驳回"
    │
    ▼
POST /api/records/:id ──→ 服务端校验
    │                        │
    │                        ▼
    │                  身份校验（HR角色）
    │                        │
    │                        ▼
    │                  状态校验（仅待审批可审批）
    │                        │
    │                        ▼
    │                  更新状态 + 审批人 + 审批时间
    │                        │
    │                        ▼
    │                  写操作日志
    │                        │
    │                        ▼
    │                  触发企微通知（通过/驳回）
    │                        │
    └────────────────────────┘
                             ▼
                       返回更新后的记录
```

### 3.3 30天回访触发逻辑

```
系统启动时 / 页面加载时
    │
    ▼
遍历所有"总结已提交"状态的记录
    │
    ▼
计算：当前日期 - 总结提交日期
    │
    ▼
┌─────────────────────────────────────────┐
│  差值 >= 30天？                          │
│  是 → 显示"提交30天自评"按钮 + 倒计时     │
│  否 → 继续等待                            │
└─────────────────────────────────────────┘
    │
    ▼
员工点击按钮 → 填写自评表单 → 提交
    │
    ▼
服务端更新：30天自评内容 + 自评提交日期
    │
    ▼
触发企微通知（30天自评已提交，请HR确认）
```

### 3.4 90天评估触发逻辑（v25新增）

```
系统启动时 / 页面加载时
    │
    ▼
遍历所有"30天已回访"状态的记录
    │
    ▼
计算：当前日期 - 30天回访日期
    │
    ▼
┌─────────────────────────────────────────┐
│  差值 >= 90天？                          │
│  是 → 显示"提交90天复盘"按钮 + 倒计时     │
│  否 → 继续等待                            │
└─────────────────────────────────────────┘
    │
    ▼
员工提交复盘 → HR查看并评估打分（1-5分）
    │
    ▼
状态更新为"90天已评估" → 企微通知
```

---

## 四、通知触发逻辑

| 通知类型 | 触发源 | 接收方 | 实现方式 |
|----------|--------|--------|----------|
| 新申请 | 员工提交申请 | 企微群（HR群） | server.js内联HTTP POST到Webhook |
| 审批通过 | HR点击通过 | 员工个人 | 同上 |
| 审批驳回 | HR点击驳回 | 员工个人 | 同上 |
| 总结已提交 | 员工提交总结 | 企微群 | 同上 |
| 30天自评提交 | 员工提交自评 | 企微群 | 同上 |
| 回访完成 | HR确认回访 | 员工个人 | 同上 |
| 90天复盘提交 | 员工提交复盘 | 企微群 | 同上 |

**注意**：所有通知都是**同步阻塞调用**，Webhook失败不会影响主流程，但也没有重试机制。

---

## 五、权限控制逻辑

```
每次API请求
    │
    ▼
提取请求头中的 Token
    │
    ▼
校验 Token 有效性（未过期？存在？）
    │
    ▼
解析 Token 获取用户身份（userId + role）
    │
    ▼
┌─────────────────────────────────────────┐
│  根据 role 决定字段权限：                  │
│  - HR：可读写所有字段                      │
│  - 员工：只能读写自己的记录 + 受限字段白名单  │
└─────────────────────────────────────────┘
    │
    ▼
操作前再次校验（如员工只能修改自己的记录）
```

---

## 六、数据持久化逻辑

```
所有写操作
    │
    ▼
先读 data.json 到内存
    │
    ▼
在内存中修改数据
    │
    ▼
将完整数据写回 data.json（全量覆盖）
    │
    ▼
同时写入操作日志（日志文件或内存数组）
```

**特点**：
- 无事务、无并发控制（单用户场景足够）
- 无数据库迁移（结构变化手动改JSON）
- 全量读写（文件小，性能可接受）

---

## 七、不存在的设计

| 概念 | 是否包含 | 说明 |
|------|----------|------|
| **长链推理（Chain-of-Thought）** | ❌ 否 | 无AI推理，纯规则引擎 |
| **多Agent协作** | ❌ 否 | 单进程单体应用 |
| **异步消息队列** | ❌ 否 | Webhook同步调用 |
| **工作流引擎** | ❌ 否 | 状态机硬编码在代码中 |
| **事件驱动架构** | ❌ 否 | 无事件总线，直接函数调用 |
| **分布式事务** | ❌ 否 | 单机文件操作 |
| **AI决策/推荐** | ❌ 否 | 无机器学习或智能分析 |

---

## 八、总结

这个系统的核心逻辑可以概括为：**状态机驱动的强制闭环 + 时间触发的倒计时催办 + Webhook通知穿透**。

技术上是一个极简的单体应用，所有业务逻辑通过代码硬编码实现，没有复杂的架构设计，但足够支撑当前的业务需求。如果未来用户量增大或需要更复杂的流程编排，可能需要引入工作流引擎或数据库。
